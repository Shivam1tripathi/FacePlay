{
  "installCommand": "if [ -d client ]; then cd client && npm install; else npm install; fi",
  "buildCommand": "if [ -d client ]; then cd client && npm run build && mv dist ../dist; else npm run build; fi",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
